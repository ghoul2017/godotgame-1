# AGENTS.md


## 核心哲学

- **学习第一**：先武装自己（调研文档、学习工具、掌握框架），再写代码
- **能力优先级**：技能 > 规则 > 工具 > 代码
- **遇到局限→调研解决**：说"做不到"之前，先搜索别人怎么解决的，找到方案立刻实施并记录
- **踩坑→写入规则**：每次遇到工具问题/语法陷阱/框架约定，当场写，不等"下次记得"
- **完全自主**：自行判断最优解并立即执行，不要反复确认
- **大胆行动**：错了就修正，不要怕错而停滞

## 遇到局限→调研解决

### "做不到"是起点，不是终点。每次遇到以下情况，必须主动搜索解决方案：
- 无法验证功能正确性→调研测试框架（GUT/gdUnit4）、截图方案、CI 工具
- 不知道某个 API 怎么用→查官方文档、搜索示例代码
- 工具不熟悉→先学再写，不要猜

### 调研结果必须记录：
- 找到方案→写入 `.claude/rules/` 或 `docs/`
- 方案落地后→写入测试验证

## 工作规则

- 全局回复使用中文。
- 开始修改前先判断任务范围、影响文件和验证方式。

## 反锚定与设计纠偏规则

- **源依据优先**：遇到设计、流程、阶段范围、UI 入口、术语命名冲突时，必须先回到源依据链，而不是沿用最近文档或现有代码。当前项目源依据链为：`docs/原始设计.txt` -> `docs/整体策划案.md` -> 专项设计文档 -> 分步实现文档 -> 代码。
- **旧内容默认可疑**：发现用户指出“理解偏差、旧实现误导、阶段错位、术语不在原设计中”时，必须把相关旧文档、旧代码和旧资源视为待审计对象，不能把它们当成默认正确的约束。
- **禁止同义替换式纠偏**：纠偏时不能只把错误词替换成新词。必须审查结构、职责、阶段归属、验收标准和文件清单；若结构错了，直接重写或删除对应章节。
- **区分最终流程和当前阶段**：最终会发生的流程不得自动写入当前阶段验收。每个阶段只写本阶段能完成、能验证、依赖已满足的内容；未来内容只能写成接口、锁定状态、调试能力或遗留问题。
- **大胆删除错误入口**：正式入口、主流程、阶段验收中出现调试入口、临时场景、旧 UI 骨架或错误概念时，必须优先移除，而不是保留后解释。
- **术语必须可追溯**：新增或重命名核心玩法术语前，必须能在源依据或用户明确表述中找到依据；找不到就不要造词，使用原文术语。
- **纠偏必须留痕验证**：完成纠偏后必须用 `rg` 检索旧词、错误入口和新规则关键字，确认偏差只出现在“禁止项、遗留问题、审计说明”中。

## 文件规则

- 新增文本文件使用 UTF-8，无 BOM。
- PowerShell 读取文本文件时使用 `Get-Content -Encoding UTF8`。
- 一般项目文档放在 `docs/`，项目入口规则放在根目录。

## 项目定位

- 引擎：Godot 4.6。
- 技术栈：Godot .NET，项目脚本默认使用 C#。
- 类型：PC 单人生存探索 / RTS，2D 俯视视角。
- 核心循环：地表探索、建造、生存 -> 建造火箭回归轨道 -> 轨道交易与解锁 -> 再次空投。
- 设计原则：玩法一致性优先，不做强制倒计时式压迫，不增加无关小游戏。

## 编码规则

- 默认使用 C# 编写运行时代码；除非明确要求，不新增 GDScript。
- C# 使用 Allman 大括号风格，优先显式类型。
- C# 文件名必须与类名大小写完全一致，使用 PascalCase，例如 `GameRoot.cs` 对应 `GameRoot`。
- 注释使用中文，只写必要解释。
- 日志前缀使用 `[模块名]`。
- 不把本机绝对路径、临时代码或个人 IDE 配置写入运行时代码。

## 核心编程思想

- 先理解问题和边界，再写代码；先确认数据流、状态所有权和失败路径。
- 保持实现简单直接，优先清晰、可读、可验证，不为未确认需求预留复杂架构。
- 单一职责优先：模块、脚本、函数只承担清晰职责，避免把输入、UI、玩法状态和资源加载混在一起。
- 数据驱动优先：数值、配置、物品、单位、建筑、事件等可变内容优先进入统一数据结构或 `Resource`。
- 显式状态优先：关键状态变化应可追踪、可调试、可复现，避免隐藏副作用和隐式全局依赖。
- 复用以降低复杂度为前提，不为了“抽象”而抽象；重复出现真实维护成本后再提炼公共逻辑。
- 面向失败编程：资源缺失、空引用、非法输入、加载失败和存档不兼容等情况要有明确处理。

## 专业程序员基本技能和素养

- 写代码前能拆解任务，识别影响范围、依赖关系、风险点和验证方式。
- 熟悉版本控制基本流程，提交前查看差异，提交内容聚焦，提交信息准确描述变更。
- 具备调试能力：能复现问题、缩小范围、阅读日志、检查调用链，并用证据定位原因。
- 具备学习能力：能快速阅读文档、示例和源码，提炼可落地的规则，并把新知识融入项目约定。
- 具备解决问题能力：能把复杂问题拆成可验证的小问题，逐步排除假设，优先找到稳定可维护的解法。
- 具备测试意识：新增或修改逻辑时考虑正常路径、边界条件、失败路径和回归风险。
- 尊重既有代码风格和项目约定，不引入无关框架、个人偏好或大规模顺手重构。
- 保持命名准确，函数和变量表达意图，避免含糊缩写和与职责不符的名称。
- 关注可维护性：控制函数长度和耦合度，避免魔法数、重复硬编码和难以追踪的全局状态。
- 沟通时说明事实、假设和风险；不确定时先澄清，不用猜测替代需求确认。

## 验证

- 修改 `project.godot` 后确认 Godot 能打开项目。
- 新增入口场景后确认 `application/run/main_scene` 指向有效 `.tscn`。
- 新增脚本后检查语法和资源路径。
- C# 代码至少运行 `dotnet build`；有 `.sln` 时优先同时验证 `dotnet build godotgame1.sln`。
- Godot .NET 构建会共享 `.godot/mono/temp` 输出，`dotnet build` 与 `.sln` 构建应按顺序运行，不要并行执行；若出现文件占用，等待进程结束后按顺序重跑，以重跑结果为准。
- Godot .NET 项目要用 Godot 控制台版或 headless 验证项目加载，例如 `tools/run_godot_console.ps1 --headless --quit`。
- 新增场景路由后，用调试参数直接进入关键场景，验证脚本能实例化、payload 能传递、日志能输出。
- 如果 `godot` 命令不可用，先搜索本机 Godot 可执行文件、IDE 配置、开始菜单快捷方式和常见安装目录；找到后补项目启动脚本。
- 无法运行 Godot 或验证工具时，只有在完成上述查找仍失败后，才在最终回复中说明。

## Git

- 不主动 push。
- 不还原用户已有改动，除非用户明确要求。
- 提交前查看 `git status --short`，确认只包含本次任务相关文件。
- commit 信息使用中文描述，例如：`docs: 添加项目入口规则`。
