# AGENTS.md

## 工作规则

- 全局回复使用中文。
- 开始修改前先判断任务范围、影响文件和验证方式。
- 涉及玩法、系统、叙事或 UI 方向时，先读 `docs/原始设计.txt`。
- 只改当前任务需要的内容，不顺手重构，不删除无关文件。
- 不确定需求边界时先说明风险并询问。
- 用户要求策划案用于落地开发时，按开发依赖顺序组织，不按最终功能分类堆模块。
- 用户明确要求“直接按最终目标走”时，不写“最小闭环 / MVP / 暂缓内容”式方案；可以分阶段，但每阶段都按最终系统形态设计。
- 用户要求子代理审核时，先完成本地成稿，再让子代理做独立审核；审核意见要落回文档或明确说明未采纳原因。
- 自动总结规则时要考虑更高层次的抽象，`找不到 godot 命令时不能直接放弃，要搜索本机可执行文件、IDE 配置、开始菜单和常见目录。`->`遇到问题要想办法解决`。

## 文件规则

- 新增文本文件使用 UTF-8，无 BOM。
- PowerShell 读取文本文件时使用 `Get-Content -Encoding UTF8`。
- 一般项目文档放在 `docs/`，项目入口规则放在根目录。

## 项目定位

- 引擎：Godot 4.6。
- 技术栈：Godot .NET，项目脚本默认使用 C#。
- 类型：PC 单人生存探索 / RTS，2D 俯视视角。
- 核心循环：地表探索、建造、生存 -> 建造火箭回归轨道 -> 轨道交易与解锁 -> 再次空投。
- 设计原则：玩法一致性优先，不做强制倒计时式压迫，不增加无关小游戏。

## 推荐目录

- `scenes/`：Godot 场景。
- `scripts/`：脚本。
- `assets/`：美术、音频、字体等资源。
- `resources/`：Godot Resource 数据。
- `docs/`：设计和技术文档。

## Godot 规则

- 入口场景必须配置到 `project.godot` 的 `application/run/main_scene`。
- 主入口只负责初始化和场景切换，玩法逻辑放到具体玩法场景。
- 2D 俯视玩法优先使用 `Node2D`、`CanvasLayer`、`TileMapLayer`、`Camera2D`。
- 输入行为统一配置到 Input Map，不在脚本中分散硬编码按键。
- 可复用配置优先使用 `Resource`，避免把大量数据硬编码进脚本。
- 程序生成内容必须保留随机种子入口，方便复现。
- Godot .NET 项目应保留 `.csproj`、`.sln`、`global.json`，方便 Godot、Rider、命令行使用同一套 .NET SDK。
- Godot C# 脚本挂到场景时，文件名必须与类名大小写完全一致，否则 Godot 会报 `Cannot instantiate C# script because the associated class could not be found`。
- 场景 `.tscn` 中的 C# 脚本路径要随文件重命名同步更新。
- 本机 Godot 不在 PATH 时，先搜索常见安装目录和磁盘，不要停在“找不到命令”；找到后优先用完整路径或项目脚本验证。

## 编码规则

- 默认使用 C# 编写运行时代码；除非明确要求，不新增 GDScript。
- GDScript 仅用于兼容已有脚本时使用 Godot 4 语法，缩进使用 Tab，文件名使用 `snake_case.gd`。
- C# 使用 Allman 大括号风格，优先显式类型。
- C# 文件名必须与类名大小写完全一致，使用 PascalCase，例如 `GameRoot.cs` 对应 `GameRoot`。
- 注释使用中文，只写必要解释。
- 日志前缀使用 `[模块名]`。
- 不把本机绝对路径、临时代码或个人 IDE 配置写入运行时代码。
- 本机绝对路径可以写入本地启动脚本或 README 的“本机启动说明”，但不得写入运行时代码和可移植资源配置。

## 核心编程思想

- 先理解问题和边界，再写代码；先确认数据流、状态所有权和失败路径。
- 保持实现简单直接，优先清晰、可读、可验证，不为未确认需求预留复杂架构。
- 单一职责优先：模块、脚本、函数只承担清晰职责，避免把输入、UI、玩法状态和资源加载混在一起。
- 数据驱动优先：数值、配置、物品、单位、建筑、事件等可变内容优先进入统一数据结构或 `Resource`。
- 显式状态优先：关键状态变化应可追踪、可调试、可复现，避免隐藏副作用和隐式全局依赖。
- 复用以降低复杂度为前提，不为了“抽象”而抽象；重复出现真实维护成本后再提炼公共逻辑。
- 面向失败编程：资源缺失、空引用、非法输入、加载失败和存档不兼容等情况要有明确处理。
- 性能优化基于证据，先保证正确性和可测性，再针对真实瓶颈优化。
- 排障时不要只报告工具缺失；应继续寻找替代路径、完整路径、配置文件、日志和可复现命令，直到得到明确证据或真正阻塞。
- 工具验证应尽量覆盖实际使用路径：命令行能 build 不代表 Godot 能实例化脚本，Godot headless 能启动也不代表 Rider 已加载项目。

## 专业程序员基本技能和素养

- 写代码前能拆解任务，识别影响范围、依赖关系、风险点和验证方式。
- 熟悉版本控制基本流程，提交前查看差异，提交内容聚焦，提交信息准确描述变更。
- 具备调试能力：能复现问题、缩小范围、阅读日志、检查调用链，并用证据定位原因。
- 具备学习能力：能快速阅读文档、示例和源码，提炼可落地的规则，并把新知识融入项目约定。
- 具备解决问题能力：能把复杂问题拆成可验证的小问题，逐步排除假设，优先找到稳定可维护的解法。
- 具备测试意识：新增或修改逻辑时考虑正常路径、边界条件、失败路径和回归风险。
- 尊重既有代码风格和项目约定，不引入无关框架、个人偏好或大规模顺手重构。
- 保持命名准确，函数和变量表达意图，避免含糊缩写和与职责不符的名称。
- 关注可维护性：控制函数长度和耦合度，避免魔法数、重复硬编码和难以追踪的全局状态。
- 沟通时说明事实、假设和风险；不确定时先澄清，不用猜测替代需求确认。

## 玩法约束

- 地表玩法围绕 RTS 操作：左键选择，右键指令，框选单位。
- 战斗重点是部署、装备和行为模式，不优先做高 APM 手操技能。
- 轨道站是永久成长层，地表远征是轮次体验。
- 资源、芯片、改装件、武器、工具应进入统一道具体系。
- 随机事件只提供变化，不强迫玩家立即响应。

## UI 与美术

- 地表 UI 参考《魔兽争霸》的清晰操作区。
- 轨道站 UI 参考《远行星号》的信息密度和功能性。
- 美术风格参考《远行星号》，避免通用科幻模板感。
- UI 首屏直接服务可玩流程，不做营销式落地页。

## 验证

- 修改 `project.godot` 后确认 Godot 能打开项目。
- 新增入口场景后确认 `application/run/main_scene` 指向有效 `.tscn`。
- 新增脚本后检查语法和资源路径。
- C# 代码至少运行 `dotnet build`；有 `.sln` 时优先同时验证 `dotnet build godotgame1.sln`。
- Godot .NET 项目要用 Godot 控制台版或 headless 验证项目加载，例如 `tools/run_godot_console.ps1 --headless --quit`。
- 新增场景路由后，用调试参数直接进入关键场景，验证脚本能实例化、payload 能传递、日志能输出。
- 如果 `godot` 命令不可用，先搜索本机 Godot 可执行文件、IDE 配置、开始菜单快捷方式和常见安装目录；找到后补项目启动脚本。
- 无法运行 Godot 或验证工具时，只有在完成上述查找仍失败后，才在最终回复中说明。

## Rider / MSBuild

- Rider 打开 Godot C# 项目时优先打开 `.sln`，不要只打开目录；否则可能出现 `No loaded projects`。
- 若 Rider 报 `MSB1013: 该响应文件被指定了两次` 或 `@.\MSBuild.rsp`，先检查仓库是否真的存在 `MSBuild.rsp`；若不存在，多半是 Rider/VS MSBuild 调用方式问题。
- Rider 的 MSBuild 工具集优先使用 .NET Core CLI / 当前 `.dotnet\dotnet.exe`，避免错误调用 Visual Studio 的 .NET Framework MSBuild。
- 用 `global.json` 锁定本项目使用的 .NET SDK 版本，降低 Rider、Godot、命令行使用不同 SDK 的概率。
- Godot 找不到 MSBuild / .NET SDK 时，使用项目启动脚本显式设置 `DOTNET_ROOT` 和 `PATH`。

## Git

- 不主动 push。
- 不还原用户已有改动，除非用户明确要求。
- 提交前查看 `git status --short`，确认只包含本次任务相关文件。
- commit 信息使用中文描述，例如：`docs: 添加项目入口规则`。
